export type LoginFormFields = {
    email: string
    password: string
}

export type RegistrationFormFields = {
    name: string
    email: string
    phoneNumber: string
    password: string
    repeatPassword: string
}
